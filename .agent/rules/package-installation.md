---
trigger: always_on
---

Whenever you add a new dependency or dev-dependency which is already using or can be used in multiple apps or packages, then always use the 'pnpm-workspace.yaml' file to add them using 'catalog:'.